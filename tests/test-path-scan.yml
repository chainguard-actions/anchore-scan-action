name: Test path scan (default sarif output)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          path: "."
          fail-build: "false"
          output-format: "sarif"
          severity-cutoff: "critical"
        continue-on-error: true

      - name: Assert sarif output path is set
        env:
          SARIF_OUT: ${{ steps.action.outputs.sarif }}
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          echo "sarif output: $SARIF_OUT"
          if [ "$OUTCOME" = "success" ]; then
            if [ -z "$SARIF_OUT" ]; then
              echo "FAIL: action succeeded but sarif output is empty"
              exit 1
            fi
            if [ ! -f "$SARIF_OUT" ]; then
              echo "FAIL: sarif output file does not exist at: $SARIF_OUT"
              exit 1
            fi
            echo "PASS: sarif output file exists at $SARIF_OUT"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (possibly network/grype download issue in harness)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
