name: Add to OSS board

on:
  issues:
    types:
      - opened
      - reopened
      - transferred
      - labeled

permissions: {}

jobs:

  run:
    uses: "anchore/workflows/.github/workflows/oss-project-board-add.yaml@48af2ac2ae466503efe50b585f3660dba2381bdd" # main
    secrets:
      token: ${{ secrets.OSS_PROJECT_GH_TOKEN }}
